require 'spec_helper'

describe Tweet::Notifier do
  it 'has a version number' do
    expect(Tweet::Notifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
